[defaults]
host_key_checking = False
inventory = unified-inventory.ini
remote_user = ec2-user

[ssh_connection]
ssh_args = -o ForwardAgent=yes

[privilege_escalation]
become = True
become_method = sudo